<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Army-GPT - AI Tools for Army Personnel</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="/static/css/common.css" rel="stylesheet">
    <style>
        .cost-display {
            transition: all 0.2s ease-in-out;
        }
        .cost-display:hover {
            transform: scale(1.02);
        }
        .cost-details {
            opacity: 0;
            transition: opacity 0.2s ease-in-out;
        }
        .cost-details:not(.hidden) {
            opacity: 1;
        }
    </style>
</head>
<body class="bg-gray-100">
    <!-- Header with Navigation -->
    <header class="bg-gray-800 text-white">
        <nav class="container mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <div class="text-xl font-bold">Army-GPT</div>
                    <div id="creditsBanner" class="hidden px-2 py-1 bg-amber-500 text-white text-sm rounded">
                        ⚠️ Using Slow and Stupid AI - Out of Money for the Fast and Smart AI
                    </div>
                </div>
                <div class="space-x-4">
                    <a href="/pace-notes" class="hover:text-gray-300 transition-colors">Pace Notes</a>
                    <a href="/policy-foo" class="hover:text-gray-300 transition-colors">Policy foo</a>
                    <a href="#" class="hover:text-gray-300 transition-colors">Coming Soon</a>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-8">
        <!-- Cost Display -->
        <div class="absolute top-20 right-4 bg-white p-4 rounded-lg shadow-md cost-display cursor-pointer">
            <div class="main-cost">
                <h3 class="text-sm font-semibold text-gray-700">Monthly Cost (CAD)</h3>
                <p class="text-lg text-gray-900">Loading...</p>
            </div>
        </div>

        <!-- Tools Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
            <!-- Pace Notes Card -->
            <div class="bg-white rounded-lg shadow-md p-6 transition-all hover:shadow-lg">
                <h2 class="text-xl font-bold mb-2">Pace Notes</h2>
                <p class="text-gray-600 mb-2">Generate pace notes for CAF Members</p>
                <p class="text-sm text-green-600 mb-4">✅ Mostly Stable</p>
                <a href="/pace-notes" 
                   class="inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition-colors">
                    Start Using
                </a>
            </div>

            <!-- Policy Assistant Card -->
            <div class="bg-white rounded-lg shadow-md p-6 transition-all hover:shadow-lg">
                <h2 class="text-xl font-bold mb-2">Policy Foo</h2>
                <p class="text-gray-600 mb-2">Search and analyze CAF policies with AI assistance</p>
                <p class="text-sm text-yellow-600 mb-4">⚠️ Proof of Concept</p>
                <a href="/policy-foo" 
                   class="inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition-colors">
                    Start Using
                </a>
            </div>
        </div>
    </main>

    <!-- Footer with Disclaimer -->
    <footer class="bg-gray-800 text-white text-center py-4">
        <p class="text-sm">
            Disclaimer: Use at your own discretion.
        </p>
    </footer>

    <!-- JavaScript -->
    <script src="/static/js/common.js"></script>
    <script src="/static/js/costs.js"></script>
    <script>
        // Initialize common features
        document.addEventListener('DOMContentLoaded', initializeCommon);
    </script>
</body>
</html> 